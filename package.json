{
	"name": "infiniai-copilot",
	"publisher": "drewzhao",
	"displayName": "InfiniAI Provider for Copilot",
	"description": "InfiniAI language model provider and diagnostics participant for VS Code",
	"icon": "assets/logo.png",
	"keywords": [
		"ai",
		"chat",
		"copilot",
		"github-copilot",
		"language-model",
		"openai",
		"infiniai",
		"anthropic",
		"claude"
	],
	"repository": {
		"type": "git",
		"url": "https://github.com/drewzhao/infini-ai-copilot.git"
	},
	"version": "0.4.0",
	"engines": {
		"vscode": "^1.117.0"
	},
	"categories": [
		"AI",
		"Chat"
	],
	"badges": [
		{
			"url": "https://img.shields.io/github/stars/drewzhao/infini-ai-copilot?style=social",
			"description": "Star infini-ai-copilot on Github",
			"href": "https://github.com/drewzhao/infini-ai-copilot"
		}
	],
	"bugs": {
		"url": "https://github.com/drewzhao/infini-ai-copilot/issues"
	},
	"license": "MIT",
	"activationEvents": [
		"onCommand:infiniai.setApikey"
	],
	"contributes": {
		"languageModelChatProviders": [
			{
				"vendor": "infiniai",
				"displayName": "InfiniAI",
				"managementCommand": "infiniai.setApikey"
			}
		],
		"commands": [
			{
				"command": "infiniai.setApikey",
				"title": "InfiniAI: Set InfiniAI API Key"
			}
		],
		"chatParticipants": [
			{
				"id": "infiniai",
				"name": "infiniai",
				"fullName": "InfiniAI",
				"description": "Diagnose and test InfiniAI language model integration",
				"commands": [
					{
						"name": "doctor",
						"description": "Check InfiniAI configuration and provider health"
					},
					{
						"name": "models",
						"description": "List discovered InfiniAI models and capabilities"
					},
					{
						"name": "test",
						"description": "Run a minimal test request against the selected InfiniAI route"
					}
				]
			}
		],
		"configuration": {
			"title": "InfiniAI Provider for VS Code",
			"properties": {
				"infiniai.plan": {
					"type": "string",
					"enum": [
						"standard",
						"coding"
					],
					"enumDescriptions": [
						"Pay-per-token billing. Uses 'infiniai.baseUrl' and 'infiniai.anthropic.baseUrl' for API requests.",
						"Coding Plan subscription. Uses 'infiniai.coding.baseUrl' and 'infiniai.coding.anthropic.baseUrl' for API requests, with a separate API key."
					],
					"description": "Select the InfiniAI billing plan. When set to 'standard', the extension uses 'infiniai.baseUrl' and 'infiniai.anthropic.baseUrl'. When set to 'coding', it uses 'infiniai.coding.baseUrl' and 'infiniai.coding.anthropic.baseUrl' instead, along with a separate API key."
				},
				"infiniai.baseUrl": {
					"type": "string",
					"default": "https://cloud.infini-ai.com/maas/v1",
					"description": "Base URL for the OpenAI-compatible API (Standard Plan). The extension appends '/chat/completions' to this value for chat requests (e.g. 'https://cloud.infini-ai.com/maas/v1/chat/completions'). Used when 'infiniai.plan' is set to 'standard'. To override, edit this value in Settings UI or add '\"infiniai.baseUrl\": \"<your-url>\"' to settings.json. Clearing a manually set value (removing it from settings.json or resetting in Settings UI) restores the default."
				},
				"infiniai.anthropic.baseUrl": {
					"type": "string",
					"default": "https://cloud.infini-ai.com/maas",
					"description": "Base URL for the Anthropic-compatible API (Standard Plan). The extension appends '/v1/messages' to this value for chat requests (e.g. 'https://cloud.infini-ai.com/maas/v1/messages'). Used when 'infiniai.plan' is set to 'standard'. To override, edit this value in Settings UI or add '\"infiniai.anthropic.baseUrl\": \"<your-url>\"' to settings.json. Clearing a manually set value (removing it from settings.json or resetting in Settings UI) restores the default."
				},
				"infiniai.coding.baseUrl": {
					"type": "string",
					"default": "https://cloud.infini-ai.com/maas/coding/v1",
					"description": "Base URL for the OpenAI-compatible API (Coding Plan). The extension appends '/chat/completions' to this value for chat requests (e.g. 'https://cloud.infini-ai.com/maas/coding/v1/chat/completions'). Used when 'infiniai.plan' is set to 'coding'. To override, edit this value in Settings UI or add '\"infiniai.coding.baseUrl\": \"<your-url>\"' to settings.json. Clearing a manually set value (removing it from settings.json or resetting in Settings UI) restores the default."
				},
				"infiniai.coding.anthropic.baseUrl": {
					"type": "string",
					"default": "https://cloud.infini-ai.com/maas/coding",
					"description": "Base URL for the Anthropic-compatible API (Coding Plan). The extension appends '/v1/messages' to this value for chat requests (e.g. 'https://cloud.infini-ai.com/maas/coding/v1/messages'). Used when 'infiniai.plan' is set to 'coding'. To override, edit this value in Settings UI or add '\"infiniai.coding.anthropic.baseUrl\": \"<your-url>\"' to settings.json. Clearing a manually set value (removing it from settings.json or resetting in Settings UI) restores the default."
				},
				"infiniai.retry": {
					"type": "object",
					"default": {
						"enabled": true,
						"max_attempts": 3,
						"interval_ms": 1000
					},
					"properties": {
						"enabled": {
							"type": "boolean",
							"default": true,
							"description": "Enable retry mechanism for api errors. Default is true."
						},
						"max_attempts": {
							"type": "number",
							"default": 3,
							"minimum": 1,
							"description": "Maximum total number of attempts, including the first request. Default is 3."
						},
						"interval_ms": {
							"type": "number",
							"default": 1000,
							"minimum": 1,
							"description": "Interval between retry attempts in milliseconds. Default is 1000 (1 seconds)."
						},
						"status_codes": {
							"type": "array",
							"items": {
								"type": "number"
							},
							"description": "Additional HTTP status codes to merge with the default retryable list [408, 429, 500, 502, 503, 504]."
						}
					},
					"description": "Retry configuration for retryable network failures and HTTP [408, 429, 500, 502, 503, 504]."
				},
				"infiniai.delay": {
					"type": "number",
					"default": 0,
					"minimum": 0,
					"description": "Fixed delay in milliseconds between consecutive requests. Default is 0 (no delay)."
				},
				"infiniai.modelDiscoveryUrl": {
					"type": "string",
					"default": "",
					"description": "Optional absolute URL for model discovery. Empty uses the selected InfiniAI plan default."
				},
				"infiniai.modelCacheTtlMs": {
					"type": "number",
					"default": 300000,
					"minimum": 0,
					"description": "How long to cache discovered models. Set 0 to refresh every request."
				},
				"infiniai.modelRoutes": {
					"type": "array",
					"default": [],
					"items": {
						"type": "object",
						"properties": {
							"pattern": {
								"type": "string"
							},
							"transport": {
								"type": "string",
								"enum": [
									"openai",
									"anthropic",
									"vertex"
								]
							},
							"baseUrl": {
								"type": "string"
							}
						},
						"required": [
							"pattern",
							"transport"
						]
					},
					"description": "Route matching overrides for model transports. The first matching pattern wins."
				},
				"infiniai.imageInputModels": {
					"type": "array",
					"default": [],
					"items": {
						"type": "string"
					},
					"description": "Force-enable image input for specific model IDs. Supports '*' wildcard patterns (e.g. 'kimi-*'). Useful when a multimodal model doesn't include a name marker like '-vision'."
				},
				"infiniai.disableImageInputModels": {
					"type": "array",
					"default": [],
					"items": {
						"type": "string"
					},
					"description": "Force-disable image input for specific model IDs. Supports '*' wildcard patterns."
				}
			}
		}
	},
	"main": "./out/extension.js",
	"scripts": {
		"vscode:prepublish": "npm run compile",
		"compile": "tsc -p ./",
		"lint": "eslint \"src/**/*.ts\"",
		"format": "prettier --write .",
		"watch": "tsc -watch -p ./",
		"test": "npm run compile && mocha \"out/**/*.test.js\"",
		"build": "npx @vscode/vsce package --baseContentUrl https://raw.githubusercontent.com/drewzhao/infini-ai-copilot/refs/heads/infini-ai-copilot/ -o infiniai-copilot-$(node -p \"require('./package.json').version\").vsix"
	},
	"devDependencies": {
		"@eslint/js": "^9.13.0",
		"@stylistic/eslint-plugin": "^2.9.0",
		"@types/mocha": "^10.0.6",
		"@types/node": "^22",
		"@types/vscode": "^1.116.0",
		"@vscode/test-cli": "^0.0.11",
		"@vscode/test-electron": "^2.5.2",
		"@vscode/vsce": "^3.7.1",
		"eslint": "^9.13.0",
		"mocha": "^11.7.5",
		"prettier": "^3.1.0",
		"typescript": "^5.9.2",
		"typescript-eslint": "^8.39.0"
	}
}
